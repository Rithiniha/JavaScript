function discount(total) {
    let amount;
if (total>100 && total <500) {
    
    amount = total - total * 0.10;
    console.log(amount);
}
else if (total > 500) {
    amount = total - total * 0.15;
    console.log(amount);
}
}
discount(200);
